# Zadna Server — دليل المشروع للمساعدين

خادم منصة زادنا للتوصيل (نابلس، فلسطين). اقرأ هذا الملف كاملاً قبل أي تعديل — يغنيك عن استكشاف المشروع.

## البنية

- **Node.js + Express + Socket.io**، قاعدة البيانات **Firebase Firestore**
- الاستضافة: **Render** — https://zadna-server.onrender.com
- مفتاح Firebase من متغير البيئة `FIREBASE_SERVICE_ACCOUNT` (JSON كامل)
- **النشر يدوي**: Render Dashboard → Manual Deploy → Deploy latest commit. لا يوجد auto-deploy (الربط عبر Public Git Repository)

```
server.js            auth + sockets + ربط الراوترات
routes/restaurants.js  المطاعم + إدارة المنيو (CRUD)
routes/orders.js       الطلبات
routes/mart.js         منتجات الهايبرماركت (108 منتج أساسي كـ fallback)
routes/partners.js     أكواد الاعتماد + الشركاء + أفضل مندوب
routes/wallet.js       محافظ المطاعم والمناديب + التسويات
routes/zones.js        مناطق التوصيل وأسعارها + حساب الأجرة
```

## قواعد العمل (مهمة — لا تكسرها)

1. **المندوب والمطعم لا يسجّلان إلا بكود اعتماد** يولّده المدير من لوحة التحكم. الكود **أحادي الاستخدام** ويُحرق داخل Firestore Transaction (منعاً للسباق). الزبون العادي يسجل بدون كود.
2. بعد تسجيل الشريك تكون حالته `pending` ولا يستطيع الدخول حتى يوافق المدير. الحالات: `pending` / `approved` / `frozen` / `rejected`.
3. **منطق الدخول = اسمح افتراضياً**: ارفض فقط `rejected` / `frozen` / `pending`. المستخدمون القدامى بلا حقل `status` يمرّون عادي — لا تشترط `=== 'approved'` أبداً (يسبب قفلاً جماعياً).
4. العمولة: **10%** من المطعم (من قيمة الطلب) و**10%** من المندوب (من أجرة التوصيل). قابلة للضبط عبر `RESTAURANT_COMMISSION` / `DRIVER_COMMISSION`.
5. **أفضل مندوب يومياً** (لا أسبوعياً) معفى من العمولة ذلك اليوم — `GET /api/top_driver`.
6. المطاعم غير المعتمدة مخفية عن الزبائن. `GET /api/restaurants` يدمج المطاعم الأساسية مع المسجلة؛ `?all=1` يُظهر المعلّقة (للمدير).
7. **أجرة التوصيل من المناطق لا من المطعم**: 18 منطقة بنابلس، كل واحدة بسعر (10–30 ₪) ومستوى بُعد `level` (0–3).
   `GET /api/delivery_quote?zone=&restaurantId=` يحسب: سعر منطقة الزبون + إضافة بُعد المطعم (0/+5/+10)،
   والإضافة تسقط إذا كان المطعم والزبون بنفس المنطقة أو الزبون أبعد. النتيجة تُقرّب لأعلى مضاعف لـ5.
   المناطق مضمّنة بالكود (تعمل حتى لو نفدت حصة Firestore)، وتعديلات المدير تُخزّن كـ overrides بمجموعة `delivery_zones`.
8. التحقق من الهاتف: فلسطيني (059/056) أو إسرائيلي (050/052/053/054/055/058)، 10 أرقام.

## غرف الشات (Socket.io) — انتبه

- `global_driver_chat` — بين المناديب فقط (وتعاميم الإدارة)
- `order_room_[orderId]` — بين الزبون والمندوب لطلب محدد
- `manager_monitor` — المدير يراقب كل الرسائل
- الغرف العامة تُستخدم **باسمها كما هي**، ولا تُسبق بـ `order_room_`. خلط هذا سبب اختلاط شات المناديب بشات الزبون سابقاً.

## ⚠️ حصة Firestore — الأهم

الخطة المجانية: **50,000 قراءة/يوم**. سبق أن نفدت الحصة بالكامل بسبب استطلاع متكرر (polling) كل 3 ثوانٍ من التطبيق واللوحة.

**القاعدة**: اعتمد على Socket.io للتحديث الفوري. أي polling يجب ألا يقل عن **30 ثانية**. قبل إضافة أي استعلام دوري، احسب استهلاكه اليومي.

## تحذيرات

- تجنّب الاستعلامات المركّبة (`where` + `orderBy` على حقلين) — تتطلب composite index وترمي 500. رتّب في الذاكرة بدلاً منها.
- `JWT_SECRET` غير مضبوط حالياً على Render → يُولَّد مفتاح مؤقت وتنتهي التوكنات عند كل إعادة تشغيل. ضبطه مطلوب قبل الإطلاق.
- عمليات الأكواد الأحادية يجب أن تبقى داخل `runTransaction`.
- المستودع القديم `yokardon/zadna-server` **مهجور**. الرسمي: `iiq2/zadna-server`.

## تطبيقات الأندرويد

`~/AndroidStudioProjects/AlOtbaApp` — Kotlin + Jetpack Compose، ثلاث نسخ (flavors): `customer` / `captain` / `merchant`.

```bash
./gradlew assembleDebug
for f in app/build/outputs/apk/*/debug/*.apk; do adb install -r "$f"; done
```

أي تعديل على التطبيقات يتطلب إعادة بناء وتنزيل — بعكس السيرفر الذي يُحدَّث بالنشر.

## لوحة تحكم المدير

`~/.gemini/antigravity/scratch/zadna-admin-web` — تطبيق Electron يفتح من أيقونة سطح المكتب. يتصل بسيرفر Render مباشرة (لا يحتاج خادماً محلياً).

## المستخدم

يزن — مبتدئ بالتيرمنال. نفّذ عنه قدر الإمكان، بسّط الشرح، وتجنّب الأوامر المعقدة.
